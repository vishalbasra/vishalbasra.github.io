 knife exec -E "Chef::ServerAPI.new(Chef::Config[:chef_server_url]).get('cookbooks/linux-base/_latest')"

rabbitmq API

git squash

python requests dynamic verb

jenkins pipeline global variables don't use def
